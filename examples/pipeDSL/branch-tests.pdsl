pipe test1(input: int<32>)[rf: int<32>[5]] {
    acquire(rf);
    if (input{0:0} == 1) {
        rf[input{4:0}] <- input;
        ---
        int<32> y = input;
        ---
        int<32> z = input;
    } else {
        int<32> x = input;
    }
    release(rf);
    ---
    call test1(input);
}

pipe test2(input: int<32>)[rf: int<32>[5]] {
    acquire(rf);
    if (input{0:0} == 1) {
        int<32> y = 4<32>;
    } else {
        int<32> y <- rf[input{4:0}];
        ---
   }
   release(rf);
   call test2(y);
}
circuit {
    r = memory(int<32>, 5);
    t = new test1(0<32>)[r];
}