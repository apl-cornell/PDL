//Expected Success
pipe unlock(inpt: int<32>)[rf: int<32>[5]<c,s>] {
    start(rf);
    int<32> x = rf[u0<5>] + inpt;
    end(rf);
    ---
    rf[u1<5>] <- x;    
    call unlock(inpt);
}

circuit {
    r = memory(int<32>, 5);
}