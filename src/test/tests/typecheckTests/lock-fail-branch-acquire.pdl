pipe test1(inpt: int<32>)[rf: int<32>[5]] {
    start(rf);
    if (inpt{0:0} == 1) {
        acquire(rf);
    }
    release(rf);
    end(rf);
    call test1(inpt);
}

circuit {
    r = memory(int<32>, 5);
}