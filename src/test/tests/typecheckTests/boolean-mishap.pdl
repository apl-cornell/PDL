pipe mistake(inpt: int<32>)[rf: int<32>[5]<c, s>] {
   bool a = inpt{0:0};
   bool b = inpt{1:1};
   if(a && b)
     {
       call mistake(inpt);
     }
   ---
   if(a && !b)
     {
       call mistake(inpt);
     }
   ---
   if((!a) && b)
     {
       call mistake(inpt);
     }
   ---
   if(!(a && b))
     {
       call mistake(inpt);
     }
}

circuit
  {
    r = memory(int<32>, 5);
  }