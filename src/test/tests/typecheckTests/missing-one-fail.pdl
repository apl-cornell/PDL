pipe quad_call(inpt: int<32>)[rf: int<32>[5]<c, s>]
  {
    bool a = inpt{0:0};
    bool b = inpt{1:1};
    bool c = inpt{2:2};
    if(a && b && c)
      {
        output(true);
      }
    ---
    if(a && b && (!c))
      {
        output(false);
      }
    ---
    if(a && (!b) && c)
      {
        output(true);
      }
    ---
    if(a && (!b) && (!c))
      {
        output(false);
      }
    ---
    if((!a) && b && c)
      {
        output(true);
      }
    ---
    if((!a) && b && (!c))
      {
        output(false);
      }
    ---
    if((!a) && (!b) && c)
      {
        output(true);
      }
  }

circuit
  {
    r = memory(int<32>, 5);
  }