//Expected Success
pipe test6(inpt: int<32>)[rf: int<32>[5]<c,s>(Queue)] {
    start(rf);
    reserve(rf);
    end(rf);
    ---
    block(rf);
    int<32> x = rf[u0<5>] + inpt;
    ---
    release(rf);
    call test6(inpt);
}

circuit {
    r = memory(int<32>, 5);
}