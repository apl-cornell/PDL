pipe test1(inpt: uint<32>)[rf: int<32>[5]] {
    uint<5> rs2 = inpt{13:9};
    if (inpt{0:0} == u1) {
        reserve(rf[rs2]);
        acquire(rf[rs2]);
        release(rf[rs2]);
    }
    call test1(inpt);
}

pipe test2(inpt: uint<32>)[rf: int<32>[5]] {
    if (inpt{0:0} == u1) {
        reserve(rf);
        acquire(rf);
        release(rf);
    }
    call test2(inpt);
}

circuit {
    r = memory(int<32>, 5);
}