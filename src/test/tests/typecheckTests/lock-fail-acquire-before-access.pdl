
pipe test6(inpt: uint<32>)[rf: int<32>[5]] {
    start(rf);
    if (inpt{0:0} == u1) {
        int<32> arg <- rf[inpt{31:27}];
        release(rf);
    } else {
        int<32> arg <- rf[inpt{31:27}];
        release(rf);
    }
    end(rf);
    call test6(inpt);
}

circuit {
    r = memory(int<32>, 5);
}