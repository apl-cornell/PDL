//Expected Fail
pipe test3(inpt: uint<32>)[rf: int<32>[5]] {
    uint<5> rs1 = inpt{0:4};
    uint<5> rs2 = inpt{5:9};
    bool a = inpt{0:0} == u1;
    start(rf);
    acquire(rf[rs1],W);
    acquire(rf[rs2],W);
    end(rf);
    if (a) {
        release(rf[rs2]);
    } else {
        release(rf[rs1]);
    }

    if (a) {
        release(rf[rs1]);
    }
    if (!a) {
        release(rf[rs2]);
    }
    call test3(inpt);
}

circuit {
    r = memory(int<32>, 5);
}