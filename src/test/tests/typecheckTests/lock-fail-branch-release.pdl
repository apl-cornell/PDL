pipe test3(inpt: int<32>)[rf: int<32>[5]] {
    start(rf);
    acquire(rf);
    if (inpt{0:0} == 1) {
        int<32> arg <- rf[cast(inpt{4:0}, uint<5>)];
        release(rf);
    }
    end(rf);
}

circuit {
    r = memory(int<32>, 5);
}