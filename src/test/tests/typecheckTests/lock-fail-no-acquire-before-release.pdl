
pipe test7(inpt: int<32>)[rf: int<32>[5]] {
    start(rf);
    if (inpt{0:0} == 1) {
        release(rf);
    } else {
        release(rf);
    }
    end(rf);
    call test7(inpt);
}

circuit {
    r = memory(int<32>, 5);
}