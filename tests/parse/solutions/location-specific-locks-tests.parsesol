pipe test1(input:int<32>)[mem:int<32>[5]<a, a>] {
  if ( input{0:0} == 1<1> ) {
      acquire(mem[input{0:4}]);
      
  } else {
      
  }
  release(mem[input{0:4}]);
  
}
pipe test2(input:int<32>)[rf:int<32>[5]<a, a>] {
  if ( input{0:0} == 1<1> ) {
      acquire(rf);
      release(rf);
      
  } else {
      
  }
  
}
circuit {
  r = memory(int<32>,5);
  mem = memory(int<32>,5);
}
