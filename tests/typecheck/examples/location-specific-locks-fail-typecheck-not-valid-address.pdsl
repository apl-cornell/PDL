pipe test1(input: int<32>)[mem: int<32>[5]] {
     int<2> rs2 = input{0:2}
     if (input{0:0} == 1) {
         acquire(mem[rs2]);
         release(mem[rs2]);
     }
 }
 
circuit {
 r = memory(int<32>, 5);
 mem = memory(int<32>, 5)
}