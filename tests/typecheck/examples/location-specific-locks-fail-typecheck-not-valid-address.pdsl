pipe test1(input: int<32>)[mem: int<32>[5]] {
     if (input{0:0} == 1) {
         acquire(mem[input{0:2}]);
         release(mem[input{0:2}]);
     }
 }
 
circuit {
 r = memory(int<32>, 5);
 mem = memory(int<32>, 5)
}